services:
  ai-chat:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_BASE_URL=https://api.openai.com/v1
      - API_KEY=${API_KEY}
      - SECRET_KEY=${SECRET_KEY}
      - USERS=${USERS}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
